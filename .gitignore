node_modules
.turbo
.next
dist
.DS_Store
coverage
.env
.env.local
.env.development
.env.production
apps/api/storage/

# Claude Code agent scratch space (worktrees, settings, transcripts)
.claude/

# LibreOffice / OpenOffice lock files — created while a doc is open.
.~lock.*#

# SQLite local éventuel (dev)
apps/api/database.sqlite

